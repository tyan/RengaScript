-----------------------------------------------------------------------
-- Type check
-----------------------------------------------------------------------
function isString(number)
	return type(number) == "string"
end

function isNumber(number)
	return type(number) == "number"
end
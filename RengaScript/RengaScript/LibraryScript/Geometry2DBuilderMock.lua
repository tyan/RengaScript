local Geometry2DBuilderMock = {}

function RectByCoord(lbx, lby, rtx, rty)
	return "RectByCoord("..tostring(lbx)..", "..tostring(lby)..", "..tostring(rtx)..", "..tostring(rty)..")"
end

return Geometry2DBuilderMock